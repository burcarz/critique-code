<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="user-attach">
            {{user.username}}
        </div>
        <div class="com-text">
            {{comment_body}}
        </div>
    </section>
    {{/each}}
</div>